# DataSource configuration for PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=20032003
spring.datasource.driver-class-name=org.postgresql.Driver
# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true  # Format SQL output
# Hibernate dialect for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=8080
server.servlet.context-path=/api
spring.application.name=swift_codes_api
# JPA and Hibernate properties
spring.jpa.properties.hibernate.id.new_generator_mappings=true
swiftcode.import.filepath=src/main/resources/SWIFT_CODES.xlsx
# File upload
file.upload-dir=./uploads


